import { requireAdmin } from "@/lib/auth"; import { db } from "@/lib/db"; import { apiError } from "@/lib/http"; import { NextResponse } from "next/server"; import { z } from "zod";
const dataSchema=z.object({content:z.string().min(5),optionA:z.string().min(1),optionB:z.string().min(1),optionC:z.string().min(1),optionD:z.string().min(1),correctOption:z.enum(["A","B","C","D"]),explanation:z.string().optional(),difficulty:z.enum(["EASY","MEDIUM","HARD"]),status:z.enum(["ACTIVE","INACTIVE"])}).partial();
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();return NextResponse.json(await db.question.update({where:{id:(await params).id},data:dataSchema.parse(await req.json())}))}catch(e){return apiError(e)}} export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();await db.question.delete({where:{id:(await params).id}});return NextResponse.json({ok:true})}catch(e){return apiError(e)}}
