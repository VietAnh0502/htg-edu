import{requireAdmin}from"@/lib/auth";import{db}from"@/lib/db";import{apiError}from"@/lib/http";import{NextResponse}from"next/server";import{z}from"zod";export async function POST(req:Request){try{await requireAdmin();const data=z.object({name:z.string().min(2),slug:z.string().regex(/^[a-z0-9-]+$/),description:z.string().optional()}).parse(await req.json());return NextResponse.json(await db.category.create({data}),{status:201})}catch(e){return apiError(e)}}
