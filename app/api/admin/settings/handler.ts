import{requireApiAdmin}from"@/lib/auth";import{db}from"@/lib/db";import{apiError}from"@/lib/http";import{NextResponse}from"next/server";import{z}from"zod";
export async function PUT(req:Request){try{await requireApiAdmin();const{key,value,isSecret}=z.object({key:z.string().regex(/^[A-Z0-9_]+$/),value:z.string().max(5000),isSecret:z.boolean().default(false)}).parse(await req.json());return NextResponse.json(await db.systemSetting.upsert({where:{key},create:{key,value,isSecret},update:{value,isSecret}}))}catch(e){return apiError(e)}}
