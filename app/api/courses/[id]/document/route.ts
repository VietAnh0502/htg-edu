import {readFile} from "node:fs/promises";import{canAccessCourse}from"@/lib/access";import{requireApiUser}from"@/lib/auth";import{protectedPdfPath}from"@/lib/course-content";import{db}from"@/lib/db";import{apiError,fail}from"@/lib/http";import{NextResponse}from"next/server";
export const dynamic="force-dynamic";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireApiUser();const{id}=await params;const course=await db.course.findUnique({where:{id},select:{id:true,slug:true}});if(!course)fail(404,"Không tìm thấy khóa học");if(!(await canAccessCourse(user.id,course.id,user.role==="ADMIN")))fail(403,"Khóa học chưa được Admin mở quyền");const filePath=protectedPdfPath(course.slug);if(!filePath)fail(404,"Khóa học chưa có tài liệu PDF");const pdf=await readFile(filePath);return new NextResponse(pdf,{headers:{"Content-Type":"application/pdf","Content-Disposition":"inline","Cache-Control":"private, no-store, max-age=0","X-Robots-Tag":"noindex, nofollow, noarchive"}})}catch(error){return apiError(error)}}
