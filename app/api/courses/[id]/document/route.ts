import{createReadStream}from"node:fs";import{stat}from"node:fs/promises";import{Readable}from"node:stream";import{canAccessCourse}from"@/lib/access";import{requireApiUser}from"@/lib/auth";import{protectedPdfPath}from"@/lib/course-content";import{db}from"@/lib/db";import{apiError,fail}from"@/lib/http";import{NextResponse}from"next/server";
export const dynamic="force-dynamic";export const runtime="nodejs";
const MAX_CHUNK=4*1024*1024;const baseHeaders={"Content-Type":"application/pdf","Content-Disposition":"inline; filename=course-material.pdf","Cache-Control":"private, no-store, max-age=0","X-Robots-Tag":"noindex, nofollow, noarchive","Accept-Ranges":"bytes"};
function pdfChunk(filePath:string,size:number,start:number,requestedEnd?:number){const end=Math.min(requestedEnd??size-1,start+MAX_CHUNK-1,size-1);const stream=Readable.toWeb(createReadStream(filePath,{start,end})) as ReadableStream;return new NextResponse(stream,{status:size>MAX_CHUNK?206:200,headers:{...baseHeaders,"Content-Length":String(end-start+1),...(size>MAX_CHUNK?{"Content-Range":`bytes ${start}-${end}/${size}`}:{})}})}
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireApiUser();const{id}=await params;const course=await db.course.findUnique({where:{id},select:{id:true,slug:true}});if(!course)fail(404,"Không tìm thấy khóa học");if(!(await canAccessCourse(user.id,course.id,user.role==="ADMIN")))fail(403,"Khóa học chưa được Admin mở quyền");const filePath=protectedPdfPath(course.slug);if(!filePath)fail(404,"Khóa học chưa có tài liệu PDF");const{size}=await stat(filePath);const range=request.headers.get("range");if(!range)return pdfChunk(filePath,size,0);const match=/^bytes=(\d*)-(\d*)$/.exec(range);if(!match)return new NextResponse(null,{status:416,headers:{...baseHeaders,"Content-Range":`bytes */${size}`}});const start=match[1]?Number(match[1]):0;const requestedEnd=match[2]?Number(match[2]):undefined;if(!Number.isSafeInteger(start)||start<0||start>=size||requestedEnd!==undefined&&(!Number.isSafeInteger(requestedEnd)||requestedEnd<start))return new NextResponse(null,{status:416,headers:{...baseHeaders,"Content-Range":`bytes */${size}`}});return pdfChunk(filePath,size,start,requestedEnd)}catch(error){return apiError(error)}}
