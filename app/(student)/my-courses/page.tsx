import Link from"next/link";import{ArrowRight,Award,BookOpenCheck,Eye,GraduationCap}from"lucide-react";import{requireUser}from"@/lib/auth";import{db}from"@/lib/db";
export const metadata={title:"Khóa học của tôi"};
export default async function MyCourses(){const user=await requireUser();const enrollments=await db.enrollment.findMany({where:{userId:user.id,status:{in:["ACTIVE","COMPLETED"]}},select:{id:true,course:{select:{id:true,title:true,slug:true,_count:{select:{questions:{where:{status:"ACTIVE"}}}},sections:{orderBy:{position:"asc"},select:{lessons:{orderBy:{position:"asc"},take:1,select:{id:true}}}},examAttempts:{where:{userId:user.id,status:{in:["SUBMITTED","EXPIRED"]}},select:{id:true,attemptNumber:true,status:true,correctCount:true,score:true,passed:true,submittedAt:true,_count:{select:{questions:true}}},orderBy:{attemptNumber:"desc"}}}}},orderBy:{enrolledAt:"desc"}});return <main className="app-shell"><section className="page-hero"><div className="container"><p className="eyebrow" style={{color:"#6ee7b7"}}>Không gian học tập</p><h1>Chào {user.name.split(" ").slice(-1)},<br/>khóa học của bạn.</h1><p style={{color:"#b8d2c8"}}>Xem video, giáo trình và tham gia bài thi cuối khóa bất cứ lúc nào.</p></div></section><section className="section" style={{paddingTop:55}}><div className="container"><div className="section-head"><div><p className="eyebrow">Nội dung đã được mở khóa</p><h2 className="section-title" style={{fontSize:38}}>Khóa học của tôi</h2></div><Link href="/courses" className="btn btn-outline">Khám phá thêm <ArrowRight size={16}/></Link></div><div className="student-course-list">{enrollments.length===0?<div className="card empty-course-card"><span className="icon-box"><GraduationCap/></span><h3>Bắt đầu khóa học đầu tiên</h3><p className="muted">Bạn chưa sở hữu khóa học nào.</p><Link href="/courses" className="btn btn-primary">Khám phá khóa học</Link></div>:enrollments.map(e=>{const firstLesson=e.course.sections.flatMap(s=>s.lessons)[0];return <article className="card student-course-card" key={e.id}><span className="icon-box"><BookOpenCheck/></span><div><span className="badge">Đã mở khóa</span><h2>{e.course.title}</h2><p className="muted">Toàn bộ video, giáo trình và lịch sử thi được lưu tại đây.</p></div><div className="student-course-actions">{firstLesson&&<Link className="btn btn-primary" href={`/learn/${e.course.slug}/${firstLesson.id}`}>Vào khóa học <ArrowRight size={16}/></Link>}{e.course._count.questions>0&&<Link className="btn btn-outline" href={`/exam/${e.course.id}`}><Award size={16}/> Làm bài thi</Link>}</div>{e.course.examAttempts.length>0&&<section className="course-attempt-history"><div className="course-attempt-heading"><h3>Lịch sử bài thi</h3><span>{e.course.examAttempts.length} lần đã hoàn thành</span></div><div className="course-attempt-list">{e.course.examAttempts.map(a=><article key={a.id}><div><b>Lần {a.attemptNumber}</b><small>{a.submittedAt?.toLocaleString("vi-VN")}</small></div><span>Đúng {a.correctCount??0}/{a._count.questions} · {a.score??0}/100</span><strong className={a.status==="EXPIRED"||!a.passed?"is-failed":"is-passed"}>{a.status==="EXPIRED"?"Hết giờ":a.passed?"Đạt":"Không đạt"}</strong><Link className="btn btn-outline btn-compact" href={`/exam-results/${a.id}`}><Eye size={14}/> Xem chi tiết</Link></article>)}</div></section>}</article>})}</div></div></section></main>}
