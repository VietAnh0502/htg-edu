"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, CheckCircle2, KeyRound } from "lucide-react";
export default function Forgot(){const [done,setDone]=useState(false);return <main className="auth-page" style={{gridTemplateColumns:"1fr"}}><section className="auth-wrap"><div className="card auth-card" style={{textAlign:done?"center":"left"}}><span className="icon-box" style={{margin:done?"0 auto 18px":undefined}}>{done?<CheckCircle2/>:<KeyRound/>}</span><h1 style={{marginTop:18}}>{done?"Kiểm tra email của bạn":"Khôi phục mật khẩu"}</h1>{done?<><p className="muted" style={{lineHeight:1.7}}>Nếu email tồn tại, hướng dẫn đặt lại mật khẩu đã được gửi.</p><Link href="/login" className="btn btn-primary"><ArrowLeft size={16}/> Về đăng nhập</Link></>:<><p className="muted" style={{marginBottom:24}}>Nhập email đã đăng ký để nhận hướng dẫn.</p><form onSubmit={async e=>{e.preventDefault();const email=new FormData(e.currentTarget).get("email");await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});setDone(true)}}><label><span className="label">Địa chỉ email</span><input className="input" type="email" name="email" required placeholder="ban@example.com"/></label><button className="btn btn-primary" style={{marginTop:16,width:"100%"}}>Gửi hướng dẫn</button></form><p style={{textAlign:"center",marginTop:20}}><Link href="/login" className="muted"><ArrowLeft size={14} style={{display:"inline"}}/> Quay lại đăng nhập</Link></p></>}</div></section></main>}
