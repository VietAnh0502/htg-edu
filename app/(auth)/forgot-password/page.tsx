"use client";
import{useState}from"react";import Link from"next/link";import{ArrowLeft,CheckCircle2,KeyRound}from"lucide-react";
export default function Forgot(){const[done,setDone]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[resetUrl,setResetUrl]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");try{const email=new FormData(e.currentTarget).get("email");const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)setError(d.error||"Không thể gửi yêu cầu");else{setResetUrl(d.resetUrl||"");setDone(true)}}catch{setError("Không thể kết nối máy chủ")}finally{setBusy(false)}}return <main className="auth-page" style={{gridTemplateColumns:"1fr"}}><section className="auth-wrap"><div className="card auth-card" style={{textAlign:done?"center":"left"}}><span className="icon-box" style={{margin:done?"0 auto 18px":undefined}}>{done?<CheckCircle2/>:<KeyRound/>}</span><h1 style={{marginTop:18}}>{done?"Kiểm tra email của bạn":"Khôi phục mật khẩu"}</h1>{done?<><p className="muted">Nếu email tồn tại, hướng dẫn đặt lại mật khẩu đã được gửi.</p>{resetUrl&&<p><Link className="btn btn-outline" href={resetUrl}>Mở liên kết thử nghiệm</Link></p>}<Link href="/login" className="btn btn-primary"><ArrowLeft size={16}/> Về đăng nhập</Link></>:<><p className="muted">Nhập email đã đăng ký để nhận hướng dẫn.</p><form onSubmit={submit}><label><span className="label">Địa chỉ email</span><input className="input" type="email" name="email" required placeholder="ban@example.com"/></label>{error&&<p role="alert" style={{color:"#dc2626"}}>{error}</p>}<button disabled={busy} className="btn btn-primary" style={{marginTop:16,width:"100%"}}>{busy?"Đang gửi...":"Gửi hướng dẫn"}</button></form><p style={{textAlign:"center",marginTop:20}}><Link href="/login" className="muted"><ArrowLeft size={14} style={{display:"inline"}}/> Quay lại đăng nhập</Link></p></>}</div></section></main>}
