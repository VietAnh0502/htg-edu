"use client";
import Link from"next/link";import{useSearchParams}from"next/navigation";import{Suspense,useState}from"react";import{KeyRound}from"lucide-react";
export default function ResetPassword(){return <Suspense fallback={<main className="auth-page"/>}><ResetPasswordForm/></Suspense>}
function ResetPasswordForm(){const params=useSearchParams();const[message,setMessage]=useState("");const[done,setDone]=useState(false);const[busy,setBusy]=useState(false);const token=params.get("token")||"";async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const payload={...Object.fromEntries(new FormData(e.currentTarget)),token};const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();setBusy(false);if(r.ok)setDone(true);else setMessage(d.error||"Không thể đặt lại mật khẩu")};return <main className="auth-page" style={{gridTemplateColumns:"1fr"}}><section className="auth-wrap"><div className="card auth-card"><span className="icon-box"><KeyRound/></span><h1 style={{marginTop:18}}>Đặt lại mật khẩu</h1>{done?<><p>Đổi mật khẩu thành công.</p><Link className="btn btn-primary" href="/login">Đăng nhập</Link></>:token?<form onSubmit={submit} style={{display:"grid",gap:16}}><label><span className="label">Mật khẩu mới</span><input className="input" name="password" type="password" minLength={8} autoComplete="new-password" required/></label><label><span className="label">Xác nhận mật khẩu</span><input className="input" name="confirmPassword" type="password" minLength={8} autoComplete="new-password" required/></label>{message&&<p role="alert" className="form-error">{message}</p>}<button className="btn btn-primary" disabled={busy}>{busy?"Đang lưu...":"Đặt lại mật khẩu"}</button></form>:<><p>Liên kết không hợp lệ.</p><Link href="/forgot-password">Yêu cầu liên kết mới</Link></>}</div></section></main>}
