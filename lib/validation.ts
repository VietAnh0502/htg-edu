import { z } from "zod";

export const courseSchema=z.object({title:z.string().trim().min(3),slug:z.string().regex(/^[a-z0-9-]+$/),courseCode:z.string().trim().min(3).max(40).regex(/^[A-Za-z0-9-]+$/).nullable(),shortDescription:z.string().trim().min(10),description:z.string().trim().min(10),targetAudience:z.string().trim().min(3),price:z.number().nonnegative().nullable().optional(),categoryId:z.string().cuid(),instructorId:z.string().cuid(),thumbnailUrl:z.union([z.string().url(),z.string().startsWith("/")]).nullable().optional(),status:z.enum(["DRAFT","PUBLISHED","ARCHIVED"]).default("DRAFT"),featured:z.boolean().default(false),examDurationMinutes:z.number().int().min(5).max(240).default(45),passingScore:z.number().int().min(1).max(100).default(70),maxAttempts:z.number().int().min(1).max(20).default(3)});
export const questionSchema=z.object({courseId:z.string().cuid(),content:z.string().trim().min(5),optionA:z.string().trim().min(1),optionB:z.string().trim().min(1),optionC:z.string().trim().min(1),optionD:z.string().trim().min(1),correctOption:z.enum(["A","B","C","D"]),explanation:z.string().trim().nullable().optional(),difficulty:z.enum(["EASY","MEDIUM","HARD"]).default("MEDIUM"),status:z.enum(["ACTIVE","INACTIVE"]).default("ACTIVE")});
